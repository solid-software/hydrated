name: hydrated_demo
description: A demo showcasing the Hydrated package.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  hydrated:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  solid_lints: ^0.0.5

flutter:
  uses-material-design: true
